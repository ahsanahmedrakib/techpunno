import PublicUsersManager from "@/features/admin/components/PublicUsersManager";

export const metadata = {
  title: "Public Users",
};

export default function AdminPublicUsers() {
  return <PublicUsersManager />;
}
